# MediaTools Video Downloader - Application Dependencies
# Located in: apps/video/downloader/requirements.txt

# Core dependencies
requests>=2.31.0
packaging>=21.0
psutil>=5.9.0

# Platform-specific dependencies
pywin32>=303; sys_platform == 'win32'

# Note: yt-dlp and FFmpeg are managed automatically by the application
# and downloaded on first run to ensure latest versions